version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - Jibril_Abdurrashid.pdf
    - styles/main.
    - images/*
    - dist/bundle.js
